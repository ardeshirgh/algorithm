INPUT
var n;
var d=2;

------------
PROCESS
if(number<2)
print"its not prime!"
number+=1
process again
of(number=2)
print"number is primr"
number+=1
if(number>2)
remainder=number%2
if(remainder=0)
it is not prime number,number+=1,divisor=2,process again 
else
divisor+=1
if(number=divisor)
print"number is prime",number+=1,divisor=2,process again
else
process again
